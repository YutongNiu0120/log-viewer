# 当前关注

## 当前状态

- 已完成项目初始化，`AGENTS.md` 与 `.project-memory/` 已建立联动。

## 下次进入建议

- 先读 `AGENTS.md` 与 `.project-memory/README.md`
- 如果要改日志读取/搜索，优先进入 `logs/`
- 如果要改服务器配置持久化，优先进入 `serverconfig/`
- 如果要改实时跟随，优先进入 `ws/`
