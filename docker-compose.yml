services:
  log-viewer:
    build: .
    restart: always
    ports:
      - "38180:38180"
    environment:
      APP_PORT: 38180
      CONFIG_FILE: /app/app-data/servers.yaml
      CONFIG_SECRET: change-me
    volumes:
      - ./app-data:/app/app-data
